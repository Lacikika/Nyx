<!DOCTYPE html>
<html>
<head>
  <title>Guild Config - <%= file %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 100%);
      color: #e2e8f0;
      min-height: 100vh;
    }
    
    header {
      background: rgba(15, 15, 35, 0.95);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(114, 137, 218, 0.2);
      box-shadow: 0 4px 32px rgba(0, 0, 0, 0.3);
      padding: 1.5rem 2rem;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    
    h1 {
      color: #e2e8f0;
      font-size: 2rem;
      font-weight: 700;
      letter-spacing: -0.025em;
      margin-bottom: 1rem;
      background: linear-gradient(135deg, #7c3aed, #3b82f6);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .nav {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }
    
    .nav a {
      color: #94a3b8;
      background: rgba(30, 41, 59, 0.5);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.875rem;
      transition: all 0.2s ease;
      border: 1px solid rgba(148, 163, 184, 0.1);
    }
    
    .nav a:hover {
      color: #e2e8f0;
      background: rgba(124, 58, 237, 0.2);
      border-color: rgba(124, 58, 237, 0.3);
      transform: translateY(-1px);
    }
    
    .container {
      max-width: 800px;
      margin: 2rem auto;
      background: rgba(15, 15, 35, 0.6);
      backdrop-filter: blur(20px);
      border-radius: 1rem;
      border: 1px solid rgba(148, 163, 184, 0.1);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
      padding: 2rem;
    }
    
    .config-header {
      text-align: center;
      margin-bottom: 2rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid rgba(148, 163, 184, 0.1);
    }
    
    .config-title {
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: inherit;
    }
    
    .config-subtitle {
      color: #94a3b8;
      font-size: 0.875rem;
    }
    
    .form-section {
      margin-bottom: 2rem;
    }
    
    .form-section:last-of-type {
      margin-bottom: 0;
    }
    
    .section-title {
      font-size: 1.125rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: inherit;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .section-icon {
      font-size: 1.25rem;
    }
    
    label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: #cbd5e1;
      font-size: 0.875rem;
    }
    
    .form-description {
      font-size: 0.75rem;
      color: #94a3b8;
      margin-bottom: 0.75rem;
      line-height: 1.4;
    }
    
    input, select {
      width: 100%;
      padding: 0.875rem 1rem;
      background: rgba(15, 15, 35, 0.8);
      color: #e2e8f0;
      border: 1px solid rgba(148, 163, 184, 0.2);
      border-radius: 0.5rem;
      font-size: 0.875rem;
      font-family: inherit;
      transition: all 0.2s ease;
    }
    
    input:focus, select:focus {
      outline: none;
      border-color: #7c3aed;
      box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.1);
    }
    
    input::placeholder {
      color: #64748b;
    }
    
    select option {
      background: #1e293b;
      color: #e2e8f0;
    }
    
    .form-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      margin-top: 2rem;
      padding-top: 2rem;
      border-top: 1px solid rgba(148, 163, 184, 0.1);
    }
    
    button {
      background: linear-gradient(135deg, #7c3aed, #3b82f6);
      color: white;
      border: none;
      border-radius: 0.5rem;
      padding: 0.875rem 2rem;
      font-weight: 600;
      font-size: 0.875rem;
      cursor: pointer;
      transition: all 0.2s ease;
      font-family: inherit;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    button:hover {
      transform: translateY(-1px);
      box-shadow: 0 10px 25px rgba(124, 58, 237, 0.3);
    }
    
    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: #94a3b8;
      background: rgba(30, 41, 59, 0.5);
      padding: 0.75rem 1rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.875rem;
      transition: all 0.2s ease;
      border: 1px solid rgba(148, 163, 184, 0.1);
    }
    
    .back-link:hover {
      color: #e2e8f0;
      background: rgba(124, 58, 237, 0.2);
      border-color: rgba(124, 58, 237, 0.3);
      transform: translateY(-1px);
    }
    
    .notification {
      padding: 1rem;
      border-radius: 0.5rem;
      margin-bottom: 1.5rem;
      font-weight: 500;
    }
    
    .notification.success {
      color: #fff;
      background: linear-gradient(135deg, #10b981, #059669);
      border: none;
    }
    
    .notification.error {
      color: #fff;
      background: linear-gradient(135deg, #ef4444, #dc2626);
      border: none;
    }
    
    .config-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
    }
    
    .config-card {
      background: rgba(30, 41, 59, 0.3);
      border-radius: 0.75rem;
      border: 1px solid rgba(148, 163, 184, 0.1);
      padding: 1.5rem;
    }
    
    @media (max-width: 768px) {
      .container {
        margin: 1rem;
        padding: 1.5rem;
      }
      
      header {
        padding: 1rem;
      }
      
      h1 {
        font-size: 1.5rem;
      }
      
      .nav {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .config-grid {
        grid-template-columns: 1fr;
      }
      
      .form-actions {
        flex-direction: column;
      }
      
      button {
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>⚙️ Guild Configuration</h1>
    <div class="nav">
      <a href="/">🏠 Főoldal</a>
      <a href="/statistics">📊 Statisztika</a>
      <a href="/blank">📄 Blank Page</a>
      <a href="/console">💻 Console</a>
      <a href="/guildconfig">⚙️ Guild Configs</a>
      <a href="/search">🔍 Search</a>
    </div>
  </header>
  
  <div class="container">
    <% if (success) { %>
      <div class="notification success">✅ <%= success %></div>
    <% } %>
    <% if (error) { %>
      <div class="notification error">❌ <%= error %></div>
    <% } %>
    
    <div class="config-header">
      <h2 class="config-title">Guild: <%= file %></h2>
      <p class="config-subtitle">Configure guild-specific settings and permissions</p>
    </div>
    
    <form method="post" action="/guildconfig/<%= file %>">
      <div class="config-grid">
        <div class="config-card">
          <div class="form-section">
            <div class="section-title">
              <span class="section-icon">👥</span>
              Staff Roles
            </div>
            <label for="staffRoles">Staff Role IDs</label>
            <p class="form-description">
              Enter role IDs separated by commas. These roles will have staff permissions.
            </p>
            <input 
              type="text" 
              id="staffRoles" 
              name="staffRoles" 
              value="<%= config.staffRoles ? config.staffRoles.join(', ') : '' %>"
              placeholder="123456789, 987654321"
            />
          </div>
        </div>
        
        <div class="config-card">
          <div class="form-section">
            <div class="section-title">
              <span class="section-icon">🎫</span>
              Request Roles
            </div>
            <label for="requestRoles">Request Role IDs</label>
            <p class="form-description">
              Roles that can make requests or use certain bot features.
            </p>
            <input 
              type="text" 
              id="requestRoles" 
              name="requestRoles" 
              value="<%= config.requestRoles ? config.requestRoles.join(', ') : '' %>"
              placeholder="111222333, 444555666"
            />
          </div>
        </div>
        
        <div class="config-card">
          <div class="form-section">
            <div class="section-title">
              <span class="section-icon">⏰</span>
              Cooldown Role
            </div>
            <label for="cooldownRole">Cooldown Role ID</label>
            <p class="form-description">
              Role applied to users during cooldown periods.
            </p>
            <input 
              type="text" 
              id="cooldownRole" 
              name="cooldownRole" 
              value="<%= config.cooldownRole || '' %>"
              placeholder="777888999"
            />
          </div>
        </div>
        
        <div class="config-card">
          <div class="form-section">
            <div class="section-title">
              <span class="section-icon">🔒</span>
              Admin Mode
            </div>
            <label for="adminOnly">Admin Only Access</label>
            <p class="form-description">
              When enabled, only administrators can use bot commands.
            </p>
            <select id="adminOnly" name="adminOnly">
              <option value="true" <%= config.adminOnly ? 'selected' : '' %>>🔒 Enabled</option>
              <option value="false" <%= !config.adminOnly ? 'selected' : '' %>>🔓 Disabled</option>
            </select>
          </div>
        </div>
      </div>
      
      <div class="form-actions">
        <button type="submit">
          <span>💾</span>
          Save Configuration
        </button>
      </div>
    </form>
    
    <a class="back-link" href="/">
      <span>←</span>
      Back to main page
    </a>
  </div>
</body>
</html>