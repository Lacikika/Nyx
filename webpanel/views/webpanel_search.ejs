<!DOCTYPE html>
<html>
<head>
  <title>Keresés eredményei - <%= q %></title>
  <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body { 
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; 
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 100%); 
      color: #e2e8f0; 
      margin: 0; 
      min-height: 100vh;
    }
    
    header { 
      background: rgba(15, 15, 35, 0.95); 
      backdrop-filter: blur(20px);
      padding: 1.5rem 2rem; 
      box-shadow: 0 4px 32px rgba(0, 0, 0, 0.3);
      border-bottom: 1px solid rgba(114, 137, 218, 0.2);
      position: sticky;
      top: 0;
      z-index: 100;
    }
    
    h1 { 
      color: #e2e8f0; 
      margin: 0 0 1rem 0; 
      font-size: 2rem; 
      font-weight: 700;
      letter-spacing: -0.025em;
      background: linear-gradient(135deg, #7c3aed, #3b82f6);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .nav { 
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }
    
    .nav a { 
      color: #94a3b8;
      background: rgba(30, 41, 59, 0.5);
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.875rem;
      transition: all 0.2s ease;
      border: 1px solid rgba(148, 163, 184, 0.1);
    }
    .nav a:hover {
      color: #e2e8f0;
      background: rgba(124, 58, 237, 0.2);
      border-color: rgba(124, 58, 237, 0.3);
      transform: translateY(-1px);
      text-decoration: underline;
    }
    
    .container { 
      max-width: 1200px; 
      margin: 2rem auto; 
      background: rgba(15, 15, 35, 0.6);
      backdrop-filter: blur(20px);
      border-radius: 1rem;
      border: 1px solid rgba(148, 163, 184, 0.1);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
      padding: 2rem;
    }
    
    .search-form {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      align-items: center;
      margin-bottom: 2rem;
      padding: 1.5rem;
      background: rgba(30, 41, 59, 0.3);
      border-radius: 0.75rem;
      border: 1px solid rgba(148, 163, 184, 0.1);
    }
    
    .search-form input {
      background: rgba(15, 15, 35, 0.8);
      color: #e2e8f0;
      border: 1px solid rgba(148, 163, 184, 0.2);
      border-radius: 0.5rem;
      padding: 0.75rem 1rem;
      font-size: 0.875rem;
      transition: all 0.2s ease;
      font-family: inherit;
    }
    
    .search-form input:focus {
      outline: none;
      border-color: #7c3aed;
      box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.1);
    }
    
    .search-form button {
      background: linear-gradient(135deg, #7c3aed, #3b82f6);
      color: white;
      border: none;
      border-radius: 0.5rem;
      padding: 0.75rem 1.5rem;
      font-weight: 600;
      font-size: 0.875rem;
      cursor: pointer;
      transition: all 0.2s ease;
      font-family: inherit;
    }
    
    .search-form button:hover {
      transform: translateY(-1px);
      box-shadow: 0 10px 25px rgba(124, 58, 237, 0.3);
    }
    .nav a:hover { text-decoration: underline; }
    
    ul { list-style: none; padding: 0; }
    
    li { 
      margin: 0.75rem 0; 
      background: rgba(30, 41, 59, 0.3);
      border-radius: 0.5rem;
      border: 1px solid rgba(148, 163, 184, 0.1);
      transition: all 0.2s ease;
    }
    
    li:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      border-color: rgba(124, 58, 237, 0.3);
    }
    
    li a { 
      color: #e2e8f0; 
      padding: 1rem 1.25rem; 
      border-radius: 0.5rem; 
      display: block; 
      text-decoration: none;
      font-weight: 500;
      transition: all 0.2s ease;
    }
    
    li a:hover { 
      color: #fff; 
    }
    
    .metadata {
      font-size: 0.875rem;
      margin-top: 0.5rem;
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }
    
    .time-badge {
      color: #94a3b8;
      background: rgba(148, 163, 184, 0.1);
      padding: 0.25rem 0.5rem;
      border-radius: 0.25rem;
      font-size: 0.75rem;
    }
    
    .role-badge {
      color: #10b981;
      background: rgba(16, 185, 129, 0.1);
      padding: 0.25rem 0.5rem;
      border-radius: 0.25rem;
      font-size: 0.75rem;
    }
    
    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: #94a3b8;
      background: rgba(30, 41, 59, 0.5);
      padding: 0.75rem 1rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.875rem;
      transition: all 0.2s ease;
      border: 1px solid rgba(148, 163, 184, 0.1);
      margin-top: 2rem;
    }
    
    .back-link:hover {
      color: #e2e8f0;
      background: rgba(124, 58, 237, 0.2);
      border-color: rgba(124, 58, 237, 0.3);
      transform: translateY(-1px);
    }
    
    .no-results {
      text-align: center;
      padding: 3rem 1rem;
      color: #94a3b8;
      font-size: 1.125rem;
    }
    
    @media (max-width: 768px) {
      .container {
        margin: 1rem;
        padding: 1rem;
      }
      
      header {
        padding: 1rem;
      }
      
      h1 {
        font-size: 1.5rem;
      }
      
      .nav {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .search-form {
        flex-direction: column;
        align-items: stretch;
      }
      
      .search-form input,
      .search-form button {
        width: 100%;
      }
      
      .metadata {
        flex-direction: column;
        gap: 0.5rem;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>Keresés eredményei: <%= q %></h1>
    <div class="nav">
      <a href="/">Főoldal</a>
      <a href="/statistics">Statisztika</a>
      <a href="/blank">Blank Page</a>
      <a href="/console">Console</a>
    </div>
  </header>
  <div class="container">
    <form method="get" action="/search" class="search-form">
      <input type="text" name="q" value="<%= q %>" placeholder="Keresés..." />
      <input type="date" name="date_from" value="<%= date_from || '' %>" />
      <input type="date" name="date_to" value="<%= date_to || '' %>" />
      <input type="text" name="role" value="<%= role || '' %>" placeholder="Szerep (role)" />
      <button type="submit">Szűrés</button>
    </form>
    
    <% if (results.length === 0) { %>
      <div class="no-results">
        <p>Nincs találat a megadott keresési feltételekre.</p>
      </div>
    <% } else { %>
      <ul>
        <% results.forEach(function(r) { %>
          <li>
            <a href="/data/<%= r.type %>/<%= r.file %>">
              <%= r.type %>/<%= r.file %>
              <div class="metadata">
                <% if (r.time) { %><span class="time-badge"><%= r.time %></span><% } %>
                <% if (r.role) { %><span class="role-badge"><%= r.role %></span><% } %>
              </div>
            </a>
          </li>
        <% }); %>
      </ul>
    <% } %>
    
    <a href="/" class="back-link">
      <span>←</span>
      Vissza a főoldalra
    </a>
  </div>
</body>
</html>